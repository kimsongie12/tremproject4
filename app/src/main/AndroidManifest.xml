<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Tremproject4"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Diary"
            android:label="Diary Activity" />
        <activity
            android:name=".Todo"
            android:label="Todo 액티비티" />
        <activity
            android:name=".Plan"
            android:label="Plan 액티비티" />
        <activity
            android:name=".Oneday"
            android:label="Oneday 액티비티" />
        <activity
            android:name=".Oneyear"
            android:label="Oneyear 액티비티" />
        <activity
            android:name=".Diaryone"
            android:label="Diaryone 액티비티" />
        <activity
            android:name=".Cal"
            android:label="CalActivity" />








    </application>

</manifest>
